/** Fixed tenant id for ATS sandbox / testing applications (public apply mirror). */
export const SANDBOX_COMPANY_ID = 'SANDBOX';

/** document id for public_profiles and routing */
export const SANDBOX_APP_SLUG = 'sandbox';
