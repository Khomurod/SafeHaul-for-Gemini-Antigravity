# WorkspaceFrame

WorkspaceFrame owns responsive application geometry, narrow-screen off-canvas
navigation, focus containment, Escape handling, focus return, and the main
content scroll region.

Features provide navigation content, route handling, permissions, the topbar,
and open/expanded state. The layout does not know route names or user roles.
