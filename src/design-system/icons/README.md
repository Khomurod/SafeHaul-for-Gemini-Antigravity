# Icons

SafeHaul currently uses `lucide-react` and a small number of branded SVGs.
This directory will define approved sizes, stroke treatment, accessible-name
rules, and business-neutral exports. Branded artwork remains an explicit
exception and must not be approximated with generic icons.
