# Lead Distribution Worker URL (REQUIRED)
# After deploying processCompanyDistribution, run:
#   gcloud run services describe processcompanydistribution --region=us-central1 --format="value(status.url)"
# Then copy this file to .env and paste the URL below:
DISTRIBUTION_WORKER_URL=https://processcompanydistribution-xxxxx-uc.a.run.app

# NOTE: Copy this file to .env and fill in actual values.
# The .env file should NOT be committed to version control.
